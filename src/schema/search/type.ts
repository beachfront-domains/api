


///  E X P O R T

export type SearchResult = {
  ascii: string;
  available: boolean;
  created: string;
  duration: number;
  hns: string;
  name: string;
  premium: boolean;
  price: string;
};



/// be sure to keep this file in sync with `schema/search.graphql`
