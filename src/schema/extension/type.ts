


///  E X P O R T

export type Extension = {
  ascii: string;
  name: string;
  registry: string;
  ///
  created: string;
  id: string;
  updated: string;
};



/// be sure to keep this file in sync with `schema/extension.graphql`
